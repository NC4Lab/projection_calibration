#define SHAPE_H

class Shape 
{
    public:

    const float DEF_WIDTH = 0.1f;
    const float DEF_HEIGHT = 0.1f;
    float Width;
    float Height;
    float X;
    float Y;

    Shape(float x = 0.0f, float y = 0.0f, float width =   )
}